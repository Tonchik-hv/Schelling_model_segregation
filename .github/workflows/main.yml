name: Schelling workflow
on:
  push
jobs:
  pusher_checker:
    runs-on: ubuntu-latest
    steps:
      - 
        name: pull repo
        uses: actions/checkout@v4
      -
        name: testing

        run: |
          chmod +x ./test.sh ./build.sh
          ./build.sh
          ./test.sh
